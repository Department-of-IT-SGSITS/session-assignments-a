import type { WeatherData, AlertSettings, LocationSettings } from '@/types';

export const currentLocation = "Bhopal";

export const currentWeather = {} as any;

export const historicalData: WeatherData[] = [];

export const alertSettings: AlertSettings = {} as any;

export const locationSettings: LocationSettings = {} as any;
