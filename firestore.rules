
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to settings and weather data
    match /(settings|weather)/{document=**} {
      allow read, write: if true;
    }
  }
}
